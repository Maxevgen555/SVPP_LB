<Window x:Class="LB7_DA.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LB7_DA"
        mc:Ignorable="d"
        Title="Добавить пациента" Height="300" Width="500" FontSize="16">
    <Grid x:Name="grid" Background="LightGreen">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Фамилия*:" Margin="5" Grid.Row="0"/>
        <Label Content="Имя*:" Margin="5" Grid.Row="1"/>
        <Label Content="Диагноз:" Margin="5" Grid.Row="2"/>
        <Label Content="Дата поступления:" Margin="5" Grid.Row="3"/>
        <Label Content="Лечащий врач:" Margin="5" Grid.Row="4"/>

        <TextBox x:Name="textBoxLastName" Margin="5" Grid.Row="0" Grid.Column="1" 
                 Text="{Binding LastName}"/>
        <TextBox x:Name="textBoxFirstName" Margin="5" Grid.Row="1" Grid.Column="1" 
                 Text="{Binding FirstName}"/>
        <TextBox x:Name="textBoxDiagnosis" Margin="5" Grid.Row="2" Grid.Column="1" 
                 Text="{Binding Diagnosis}"/>
        <TextBox x:Name="textBoxAdmissionDate" Margin="5" Grid.Row="3" Grid.Column="1" 
                 Text="{Binding AdmissionDate}" ToolTip="Формат: ГГГГ-ММ-ДД"/>
        <TextBox x:Name="textBoxDoctor" Margin="5" Grid.Row="4" Grid.Column="1" 
                 Text="{Binding DoctorInCharge}"/>

        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Center" Margin="10">
            <Button x:Name="buttonOk" Content="OK" Width="80" Margin="10" Click="buttonOk_Click"/>
            <Button x:Name="buttonCancel" Content="Отмена" Width="80" Margin="10" Click="buttonCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>