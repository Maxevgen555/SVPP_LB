<Window x:Class="LB7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LB7"
        mc:Ignorable="d"
        Title="Учет пациентов" Height="600" Width="900">
    <DockPanel LastChildFill="True" Background="Bisque">
        <StackPanel Background="LightGreen" Width="150">
            <Button x:Name="buttonView" Height="50" Margin="10" Click="buttonView_Click">Просмотр</Button>
            <Button x:Name="buttonInsert" Height="50" Margin="10" Click="buttonInsert_Click">Вставить</Button>
            <Button x:Name="buttonFind" Height="50" Margin="10" Click="buttonFind_Click">Найти</Button>
            <Button x:Name="buttonChange" Height="50" Margin="10" Click="buttonChange_Click">Обновить</Button>
            <Button x:Name="buttonDelete" Height="50" Margin="10" Click="buttonDelete_Click">Удалить</Button>
        </StackPanel>

        <StackPanel x:Name="stackpanelPatient" DockPanel.Dock="Top" Background="LightSkyBlue">
            <StackPanel Orientation="Horizontal">
                <Label Width="200" Margin="10">Фамилия</Label>
                <TextBox x:Name="textBoxLastName" Width="200" Margin="10" Text="{Binding Path=LastName}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Width="200" Margin="10">Имя</Label>
                <TextBox x:Name="textBoxFirstName" Width="200" Margin="10" Text="{Binding Path=FirstName}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Width="200" Margin="10">Диагноз</Label>
                <TextBox x:Name="textBoxDiagnosis" Width="200" Margin="10" Text="{Binding Path=Diagnosis}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Width="200" Margin="10">Дата поступления</Label>
                <TextBox x:Name="textBoxAdmissionDate" Width="200" Margin="10" Text="{Binding Path=AdmissionDate}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Width="200" Margin="10">Лечащий врач</Label>
                <TextBox x:Name="textBoxDoctor" Width="200" Margin="10" Text="{Binding Path=DoctorInCharge}"/>
            </StackPanel>

            <!-- Панель поиска -->
            <StackPanel Orientation="Horizontal" Margin="10" Background="LightYellow">
                <Label Width="100">Поиск по:</Label>
                <ComboBox x:Name="comboSearchBy" Width="120" Margin="5" SelectedIndex="0">
                    <ComboBoxItem Content="Фамилия"/>
                    <ComboBoxItem Content="Имя"/>
                    <ComboBoxItem Content="Диагноз"/>
                    <ComboBoxItem Content="Врач"/>
                    <ComboBoxItem Content="ID"/>
                </ComboBox>
                <TextBox x:Name="textBoxSearch" Width="200" Margin="5" />
                <Button x:Name="buttonSearch" Content="Найти" Margin="5" Click="buttonFind_Click"/>
            </StackPanel>
        </StackPanel>

        <ListBox x:Name="listBox" ItemsSource="{Binding}" Margin="10" Background="Beige" 
                 SelectionChanged="listBox_SelectionChanged"/>
    </DockPanel>
</Window>