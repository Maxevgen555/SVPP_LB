<Window x:Class="LB8.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование" Height="280" Width="280">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Content="Фамилия:" Grid.Row="0" Margin="5"/>
        <Label Content="Имя:" Grid.Row="1" Margin="5"/>
        <Label Content="Диагноз:" Grid.Row="2" Margin="5"/>
        <Label Content="Дата:" Grid.Row="3" Margin="5"/>
        <Label Content="Врач:" Grid.Row="4" Margin="5"/>

        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding LastName}"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding FirstName}"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Diagnosis}"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding AdmissionDate}"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="5" Text="{Binding DoctorInCharge}"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnOK" Content="OK" Width="80" Margin="5" Click="btnOK_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="80" Margin="5" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>