<Window x:Class="LB8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Учет пациентов" Height="400" Width="950">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="10" Width="140">
            <Button x:Name="btnAdd" Click="btnAdd_Click" Content="Добавить" Margin="5"/>
            <Button x:Name="btnDelete" Click="btnDelete_Click" Content="Удалить" Margin="5"/>
            <Button x:Name="btnEdit" Click="btnEdit_Click" Content="Редактировать" Margin="5"/>
            <Button x:Name="btnRefresh" Click="btnRefresh_Click" Content="Обновить" Margin="5"/>
        </StackPanel>

        <DataGrid x:Name="dGrid" Grid.Column="1" Margin="10" AutoGenerateColumns="False"
                 LoadingRow="dGrid_LoadingRow" IsReadOnly="True" RowHeaderWidth="50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Диагноз" Binding="{Binding Diagnosis}" Width="200"/>
                <DataGridTextColumn Header="Дата поступления" Binding="{Binding AdmissionDate}" Width="120"/>
                <DataGridTextColumn Header="Лечащий врач" Binding="{Binding DoctorInCharge}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>